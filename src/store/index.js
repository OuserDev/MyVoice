import { createStore } from 'vuex'
import {mapState} from 'vuex';

export default createStore({
  state(){
		return {
			login_on: 0
		}
	},
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})